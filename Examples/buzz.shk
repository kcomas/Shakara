fizzbuzz = (number)
{
	buzzer = ""

	if (number % 3 == 0)
		buzzer += "Fizz"

	if (number % 5 == 0)
		buzzer += "Buzz"

	if (buzzer == "")
		return number

	return buzzer
}

count = 1

while (count <= 100)
{
	print(fizzbuzz(count), " ")

	count++
}