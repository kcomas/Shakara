fizzbuzz = (number)
{
	buzzer = ""

	if (number % 3 == 0)
		buzzer += "Fizz"
	else if (number % 5 == 0)
		buzzer += "Buzz"
	else if (buzzer == "")
		return number
	else
		return buzzer
}

count = 1

while (count <= 100)
{
	print(fizzbuzz(count), " ")

	count++
}
